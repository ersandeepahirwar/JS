const first = (event) => {
  console.log(`Event Name : ${event.type} & Event Value : ${event.key}`);
};

const second = (event) => {
  console.log(`Event Name : ${event.type} & Event Value : ${event.key}`);
};

const third = (event) => {
  console.log(`Event Name : ${event.type} & Event Value : ${event.key}`);
};
