/*

Write a JavaScript program that checks whether a person can drive based on their age.
If the age is 18 or older, print "You can drive".
Otherwise, print "You can't drive".

Note - Use the ternary operator.

*/

const age = 25;

console.log(age >= 18 ? "You can drive" : "You can't drive");
