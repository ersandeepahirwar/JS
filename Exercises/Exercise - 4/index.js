// Use logical operators in JavaScript to check whether a person’s age lies between 10 and 20.

const age = 25;

console.log(
  age > 10 && age < 20
    ? "Person's age lies between 10 and 20"
    : "Person's age does not lie between 10 and 20"
);
