/* 

Create a constant object in JavaScript.
Then, try adding a new key–value pair to the same object.

*/

const student = {
  name: "Sandeep Ahirwar",
  age: 25,
};

student["height"] = 5.7;

console.log(student);
