// Write a JavaScript program to calculate the area of a right angle triangle.

const base = 20;
const height = 10;

const area = (1 / 2) * base * height;

console.log(
  `For base ${base} and height ${height}, the area of right angle triangle is ${area}`,
);
