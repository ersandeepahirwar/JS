// Write a JavaScript program to calculate the area of a circle.

const PI = 3.14;
const r = 25;

const A = (PI * Math.pow(r, 2)).toFixed(2);
console.log(`For radius ${r}, the area of the circle is ${A}`);
