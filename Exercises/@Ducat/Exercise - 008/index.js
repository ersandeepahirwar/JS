// Write a JavaScript program to convert temperature from Celsius to Fahrenheit.

const temperatureC = 37;

const temperatureF = (temperatureC * (9 / 5) + 32).toFixed(1);
console.log(`${temperatureC}°C = ${temperatureF}°F`);
