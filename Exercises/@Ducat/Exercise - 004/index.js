// Write a JavaScript program to calculate the area of a cuboid.

const l = 10;
const b = 5;
const h = 8;

const A = 2 * (l * b + b * h + h * l);

console.log(
  `For length ${l}, breadth ${b}, and height ${h}, the area of cuboid is ${A}`,
);
